"use client";

import { DottedSurface } from "@/components/ui/dotted-surface";

/** Global dotted wave background (21st.dev / sshahaider). */
export function SiteBackground() {
  return <DottedSurface />;
}
